// Story 80 / ADR 0078: the curator-only "Remove from catalog" action on a
// community-promoted book. STUB (red): renders nothing until implementation.
export function DemoteControl(_props: {
  bookSlug: string;
  source?: string;
  canCurate: boolean;
}) {
  return null;
}
