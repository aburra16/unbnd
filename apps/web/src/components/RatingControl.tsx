// Book-detail rating control. ADR 0005. Sovereign session: 1-5 stars +
// optional review -> template -> NIP-07 signEvent -> submit. Anonymous: a
// sign-in prompt. Custodial: a "coming for email accounts" note (5b).
// STUB: the Implementer builds the real control; this renders nothing so the
// component suite fails for the right reason during test design.
export function RatingControl(_props: { bookSlug: string }) {
  return null;
}
