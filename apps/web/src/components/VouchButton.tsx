// Story 68 / ADR 0067 — placeholder stub. The real control vouches a profile owner
// as a curator (tier-branched write, clones FollowButton): rendered only when
// signed-in, vouch-eligible (api.profile.meCurator.canVouch), and viewing another
// profile; toggles Vouch / Vouched (withdraw); optimistic + revert.
export function VouchButton(_props: { target: string }) {
  return null;
}
