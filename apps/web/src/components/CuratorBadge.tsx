// Story 67 / ADR 0066 — placeholder stub. The real badge resolves the profile owner's
// curator status (api.profile.curatorStatus) and renders a neutral "Curator"
// label (@unbnd/ui, token-only) when they are a curator; nothing otherwise.
export function CuratorBadge(_props: { npub: string }) {
  return null;
}
