// Story 68 / ADR 0067 — placeholder stub. The real link renders a "Curate" nav
// entry to /submissions only for a signed-in curator (api.profile.meCurator.isCurator).
export function CurateNavLink() {
  return null;
}
