// Story 70 / ADR 0068 — placeholder stub. The real indicator classifies the
// hype gap (classifyHypeGap) and renders a token-only colour+text signal for
// hidden-gem / overhyped; nothing for consensus / below the trusted-rater minimum.
export function HypeGapIndicator(_props: {
  rawAverage: number | null;
  trustedAverage: number | null;
  trustedCount: number;
}) {
  return null;
}
