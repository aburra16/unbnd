// STUB (red) — Story 94 / ADR 0085.
import type { GuideSection } from "./load";

export function GuideTree(_props: {
  sections: readonly GuideSection[];
  currentSlug?: string;
  activeAnchor?: string;
}) {
  return null;
}
