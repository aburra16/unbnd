// CLI: load Appendix M from the style guide, expand the content globs, scan,
// print hits, exit non-zero only on error-severity hits (ADR 0080 §2).
// STUB (red): wired in implementation.
process.exit(0);
