# Review: Story 86 — Reference: getting started + finding books

**Reviewer:** Claude (acting as Reviewer)
**Date:** 2026-06-12
**Diff:** `git diff main...HEAD`

## Quality gates (run by reviewer, not trusted)
- [x] typecheck 0 · full suite green (incl. the new standing content-integrity guard over the 10 real entries: anchors unique, related refs resolve, bodies non-trivial) · build ok · **scan re-run independently: 11 files, 0 errors, 2 flags.**

## The writing process
- [x] The two-pass shape holds and improved on #85's proving run: **the draft commit honestly carries a machine-caught error** (F3 "actually") and the edit-pass commit fixes it alongside the judgment edits, so the diff shows both the machine and the human working.
- [x] The edit pass is real and itemized: a B7 range cut, an accuracy fix (thriller is a genre, the draft said mood), and a staleness class I want to name for future batches: **forward references to unpublished sections must be plain text, not links, and must not carry "once published" scaffolding clauses** that go stale silently. Two were cut here; batch authors should not write them.
- [x] The 2 remaining flags are the on-screen feature name "Explore genres," quoted as the screen shows it — the sense-dependent C1 flag cleared by exactly the judgment it exists for.
- [x] Judgment spot-read: no we-voice anywhere (reference entries); the one triad in `what-unbnd-is` (the three views) is three distinct facts, the section's only one; steps are imperative where they are how-tos and positional where the book-page tour is a tour; the read-aloud test passes on the openings; the dignity register holds.

## Spec adherence
- [x] All 10 inventory entries for sections 1–2 published under stable authored anchors, named by on-screen words where screen words exist ("Hidden Gems," "For You," "Search").
- [x] The sign-in entry never uses the protocol word despite the on-screen button carrying it: it describes the second option by what it does ("a portable reading account from the wider network Unbnd is part of... a small browser extension") and routes the curious to the marked #91 note. The brief's own AC, satisfied as written.
- [x] The landing contents grew its first two sections automatically (the #84 published-only rule, untouched); moves 1–3 link this batch's entries; move 4 (follow) waits for #87's followers entry, recorded here and in the story.
- [x] The book-page tour's sideways trust links deferred to #87 per the promotion-time seam; no dead anchors exist (the integrity guard proves the frontmatter refs; the two inline body links target published entries).

## Findings
### Blocking
_None._
### Non-blocking
1. **The staleness rule above** deserves a line in the authoring README so batch drafts stop producing it; fold into #87.
2. Landing move 4 unlinked until #87 (tracked there).
3. The visual baselines are untouched and correctly so: no captured screen renders guide content.

## Verdict
**PASS** — the first content batch lands with the process intact: an honest draft, a recorded reasoned edit pass, the standing integrity guard, the wall at zero exemptions, and entries that read like a person wrote them.
